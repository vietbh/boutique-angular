import {route} from './router.js';
// let myApp = angular.modules('myApp',['ngRoute']);

// 

myApp.controller('myController', function($rootScope, $scope) {
    // sử dụng $rootScope trong đây
});
export {main}